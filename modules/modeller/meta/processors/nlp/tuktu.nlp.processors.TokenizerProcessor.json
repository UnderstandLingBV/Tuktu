{
	"name": "Tokenizer Processor",
	"description": "Tokenizes a piece of data in a given field.",
	"class": "tuktu.nlp.processors.TokenizerProcessor",
	"parameters": [
		{
			"name": "id",
			"type": "string",
			"required": true
		},
		{
			"name": "result",
			"type": "string",
			"required": true
		},
		{
			"name": "config",
			"type": "object",
			"required": true,
			"parameters": [
				{
					"name": "field",
					"description": "The field to be tokenized. The field's content must be a String or a JsString.",
					"type": "string",
					"required": true
				},
				{
					"name": "as_string",
					"description": "If false, an Array of Tokens (Strings) is returned; otherwise a String is returned where the tokens are separated by blanks (' ').",
					"type": "boolean",
					"required": false,
					"default": false
				},
				{
					"name": "language",
					"description": "If given, a language-specific tokenizer may be applied if available. Will fall back to default if this field is not given or if the language-specific tokenizer is unavailable.",
					"type": "string",
					"required": false
				}
			]
		}
	]
}