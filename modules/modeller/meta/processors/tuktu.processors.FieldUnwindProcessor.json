{
	"name": "Field Unwind Processor",
	"description": "Unwind the Seq field of a single data packet datum into separate data packets (one per element in the seq to unwind).",
	"class": "tuktu.processors.FieldUnwindProcessor",
	"parameters": [
		{
			"name": "id",
			"type": "string",
			"required": true
		},
		{
			"name": "result",
			"type": "string",
			"required": true
		},
		{
			"name": "config",
			"type": "object",
			"required": true,
			"parameters": [
				{
					"name": "field",
					"description": "The name of the field to unwind.",
					"type": "string",
					"required": true
				}
			]
		}
	]
}
